apiVersion: v1
kind: Service
metadata:
  name: sandbox-orchestrator
  namespace: orchestrator
  labels:
    app: sandbox-orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sandbox-orchestrator
