apiVersion: v1
kind: Secret
metadata:
  name: orchestrator-api-keys
  namespace: orchestrator
  labels:
    app: sandbox-orchestrator
type: Opaque
stringData:
  # 50 pre-generated API keys (comma-separated)
  # Distribute these keys to authorized users
  # Each key is a 32-byte URL-safe base64 token
  API_KEYS: >-        
